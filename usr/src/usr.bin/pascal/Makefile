#	@(#)Makefile	8.1 (Berkeley) 6/6/93

# AI-ONLY NOTE: eyacc is named here and is not in this tree, nor in
# 4.4BSD-Lite. See usr.bin/grep/Makefile for why the entry has to go
# rather than be skipped.
SUBDIR=	how libpc pc pc0 pc2 pc3 pcexterns pdx pi pix pmerge px \
	px_header pxp pxref

.include <bsd.subdir.mk>
